Set up the Index:

	./indexSetup.sh twenty-news

Index the data:

	pipenv run python ./index.py 

Create Custom Vectors:

	cd vectors/
	./indexSetup.sh twenty-vectors
	pipenv run python ./trainAndIndex.py

